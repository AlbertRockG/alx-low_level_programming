#include "main.h"

/**
 * _sqrt_recursion - It computes the natural square root of a number.
 * @n: The given number.
 *
 * Return: if n >= 0, the natural square root.
 *         if not, - 1.
 */
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);
	else if
