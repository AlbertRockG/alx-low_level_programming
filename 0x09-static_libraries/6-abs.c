/*
 * File: 6-abs.c
 * Author: AlbertG
 */

#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @n: integer to check.
 *
 * Return: n if n >= 0, otherwise -n.
 */
int _abs(int n)
{
	if (n >= 0)

		return (n);
	else
		return (-n);
}
